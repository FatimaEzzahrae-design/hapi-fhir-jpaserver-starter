apiVersion: v1
kind: Service
metadata:
  name: pushgateway
  labels:
    app: pushgateway
    release: monitoring
spec:
  type: NodePort
  selector:
    app: pushgateway
  ports:
    - name: http
      port: 9091
      targetPort: 9091
      nodePort: 30901